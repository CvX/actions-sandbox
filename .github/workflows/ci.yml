name: Update CI

on:
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    container:
      image: node:latest

    services:
      echo:
        image: hashicorp/http-echo
        ports:
          - 80:5678

    steps:
      - run: curl http://echo
